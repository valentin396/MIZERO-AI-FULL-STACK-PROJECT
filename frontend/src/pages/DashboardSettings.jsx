export default function DashboardSettings() {
  return (
    <div className="px-8 py-8 max-w-md">
      <h1 className="text-2xl font-semibold mb-1">Settings</h1>
      <p className="text-ink/60">
        Password changes, notification preferences, and account deletion would
        live here in a production build — kept out of this pass to stay
        focused on the core matching features.
      </p>
    </div>
  );
}
